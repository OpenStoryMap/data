Scripts in this directory have been run using python 3.8.10.

To run, create a virtual environment and run `pip install -r requirements.txt`.

Each script has details at the beginning of the file to run properly.
